apiVersion: v1
kind: Service
metadata:
  name: identity-provider-service
  namespace: tc-namespace
spec:
  selector:
    app: identity-provider
    tier: backend
  ports:
  - protocol: TCP
    port: 8090
    targetPort: 8090

